export default defineAppConfig({
    ui: {
        strategy: 'override',
        primary: 'violet',
        divider: {
            border: {
                base: 'flex border-white-200 dark:border-white-800',
            },
        }
    }
})